题干：给定一个由整数组成的非空数组所表示的非负整数，在该数的基础上加一。
最高位数字存放在数组的首位， 数组中每个元素只存储单个数字。
你可以假设除了整数 0 之外，这个整数不会以零开头。

解法一：如果数组最后一个元素不为9，则使其加一后返回该数组，如果等于9，则将该元素
置0然后向前继续判断，若不为9，+1返回，若为9，则置0继续向前，直至到第一个元素时，若
仍为9，则返回一个digits.Length+1长度的，第一个元素为1，其他元素为0的数组