题干：
给定一个包含红色、白色和蓝色，一共 n 个元素的数组，原地对它们进行排序，使得相同颜色的元素相邻，并按照红色、白色、蓝色顺序排列。

此题中，我们使用整数 0、 1 和 2 分别表示红色、白色和蓝色。

注意:
不能使用代码库中的排序函数来解决这道题。

解法一：
遍历整个数组，分别记录0，1，2的个数，再遍历第二遍，将相应数量的0，1，2逐次替换数组中的元素

解法二：
设置两个指针分别指向数组头尾，遍历整个数组，为0则替换表头指针指向的元素，为2则替换表尾指针指向的元素，为1则不予操作