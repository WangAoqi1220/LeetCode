题干：
用两个栈实现一个队列。队列的声明如下，请实现它的两个函数 appendTail 和 deleteHead ，分别完成在队列尾部插入整数和在队列头部删除整数的功能。(若队列中没有元素，deleteHead 操作返回 -1 )

解法：
创建两个栈，当Push一个元素时，放进Stack1，Pop一个元素时，查看Stack2是否为空，不为空则输出Stack2顶端元素，若为空，则将Stack1依次放置进Stack2，再输出，若这样操作之后Stack2仍为空，则说明Stack1原本也为空，则输出-1