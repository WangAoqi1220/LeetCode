题干：给定一个只包括 '('，')'，'{'，'}'，'['，']' 的字符串，判断字符串是否有效。

有效字符串需满足：

左括号必须用相同类型的右括号闭合。
左括号必须以正确的顺序闭合。
注意空字符串可被认为是有效字符串。

解法：
当输入为空时，返回true，运用堆栈来实现，当输入'('，'{'，'['时将其压入栈中，当输入
')'，'}'，']'时检查栈顶是否为对应的前括号，若不是，则返回false，若是，则将栈顶元
素推出，继续处理下一个输入，最后检测栈内是否还有元素，若为空，则返回true。