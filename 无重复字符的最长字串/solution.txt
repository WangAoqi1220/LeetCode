题干：给定一个字符串，请你找出其中不含有重复字符的 最长子串 的长度
解法：当字符串长度等于0时直接返回0
大于0时，初始maxlen为1，设置头尾两个游标，来标记当前在计算的字符子串，用IndexOf
判断当前字符在索引在start到end之间是否有出现过，若出现过，则将游标start移动至前
一个重复字符的后一个位置，然后计算end-start+1（当前操作的字符子串的长度），让它
跟maxlen比较，取更大的值，最后返回maxlen
